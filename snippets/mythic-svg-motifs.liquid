{% comment %}
  SVG motifs for dragons, dice, and tomes. These are used in header, hero, etc.
  You can replace these with your own SVGs later.
{% endcomment %}
<svg width="0" height="0" style="position:absolute;">
  <defs>
    <!-- Example: Gold dragon motif -->
    <linearGradient id="mythic-gold" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#f4c542"/>
      <stop offset="100%" stop-color="#d1a022"/>
    </linearGradient>
  </defs>
</svg>
<!-- Example SVG dragon using mythic gold gradient -->
<svg class="motif-dragon" width="64" height="48" viewBox="0 0 64 48">
  <path d="M32 10 L54 44 L32 30 L10 44 Z" fill="url(#mythic-gold)" stroke="#d1a022" stroke-width="2"/>
  <!-- Replace with detailed SVG later -->
</svg>